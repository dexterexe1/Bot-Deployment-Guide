I'm debugging a Discord OAuth login flow in an Express + React (TanStack Router) app deployed on Render.

Problem

The login route works, the OAuth state is saved, and MongoDB sessions are configured.

Logs:

LOGIN
Session ID: y5rBN45_FI4oV0X6MI4uoYnrCIAYnEnl
State: 66339c42-f149-4bb5-a786-375673c26146

After save:
oauthState: 66339c42-f149-4bb5-a786-375673c26146

CALLBACK
Cookie header: undefined
Session ID: gc-pvo0xIC3YOd1E2QoXK3XmfUtoZCi5
Session state: undefined

The callback always throws:

OAUTH_STATE_MISMATCH

because a completely new session is created.

Current setup
Express
express-session
connect-mongo
MongoDB connected successfully
Render deployment
Discord OAuth
Session cookie name: ubdash.sid

Session config:

session({
  name: env.SESSION_COOKIE_NAME,
  secret: env.SESSION_SECRET,
  resave: false,
  saveUninitialized: false,
  rolling: true,
  store: MongoStore.create(...),
  cookie: {
    httpOnly: true,
    secure: env.NODE_ENV === "production",
    sameSite: "lax",
    maxAge: env.SESSION_TTL_MS,
  },
})

Login route:

req.session.oauthState = state;

req.session.save(err => {
    if (err) return next(err);
    res.redirect(discordUrl);
});
Important observation

The browser receives the session cookie on /discord/login, but during the callback request:

req.headers.cookie === undefined

so Express creates a brand new session.

The frontend is deployed separately from the API on Render.

Frontend:

https://united-bunnies-4md6.onrender.com

API:

https://united-bunnies-1.onrender.com

The frontend currently redirects with:

window.location.href = "/api/v1/auth/discord/login";
Task

Find the root cause of why the session cookie is not sent to the callback.

Check for:

Cross-origin OAuth issues.
Render proxy configuration.
Vite proxy configuration.
Cookie domain/path issues.
Whether frontend and backend being on different origins is breaking sessions.
Whether sameSite, secure, or trust proxy need changes.
Whether the frontend should call the API origin directly or use a reverse proxy.
Whether the callback URL or login URL should use the same origin.
Any mistakes in the Express session configuration.

Please provide the exact code changes needed to make Discord OAuth preserve the session correctly and eliminate the OAUTH_STATE_MISMATCH error.